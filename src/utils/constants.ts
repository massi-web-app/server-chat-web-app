export enum Routes {
  AUTH = "auth",
  USERS = "users",
  CONVERSATIONS = "conversations",
  PARTICIPANTS = "participants",
}

export enum Services {
  AUTH = "AUTH_SERVICES",
  USERS = "USERS_SERVICES",
  COVNERSATIONS = "CONVESATIONS_SERVICE",
  PARTICIPANTS = "PARTICIPANT_SERVICE"
}
